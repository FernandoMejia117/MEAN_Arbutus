import { FilterCormaPipe } from './filter-corma.pipe';

describe('FilterCormaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCormaPipe();
    expect(pipe).toBeTruthy();
  });
});
